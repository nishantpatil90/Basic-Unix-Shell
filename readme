This is the basic Unix shell implementation in C for understating purpose of how shell works
We have implemented cd using chdir sys call
For history, a file is created which has all the commands entered on our shell stored and can be retrived when the called.
